{
  "buildCommand": "bash vercel-build.sh",
  "outputDirectory": "target/release",
  "installCommand": "apt-get update && apt-get install -y build-essential pkg-config libssl-dev git 2>/dev/null || echo 'Build tools unavailable'",
  "framework": null,
  "env": {
    "CARGO_PROFILE_RELEASE_OPT_LEVEL": "z",
    "CARGO_BUILD_JOBS": "2"
  },
  "regions": ["iad1"],
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}

